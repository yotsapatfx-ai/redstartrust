# PROJECT PLAN — RedStarTrust

**เอกสารแผนโปรเจ็คสำหรับการตรวจสอบภายนอก**
**ฉบับ 1.0 · 3 กันยายน 2569 (2026) · จัดทำโดย Noting (นักออกแบบ UX/UI) ตามคำสั่ง Boss**

> **จุดประสงค์ของเอกสารนี้:** ให้หน่วยงานตรวจสอบเห็นภาพครบว่าโปรเจ็คนี้ทำอะไร ไปถึงไหนแล้ว
> ควบคุมคุณภาพด้วยกลไกอะไร และอะไรที่ **ยังไม่ได้ทำ / ยังตัดสินใจไม่ได้ / ยังพิสูจน์ไม่ได้**
> ทุกข้อความในเอกสารนี้ตรวจสอบย้อนกลับได้จากไฟล์จริงในรีโปที่ระบุไว้ท้ายแต่ละหัวข้อ

---

## 0. สิ่งที่ผู้ตรวจต้องอ่านก่อนอย่างอื่น

เพื่อไม่ให้เข้าใจสถานะโปรเจ็คคลาดเคลื่อน ขอระบุ 5 ข้อนี้ไว้ก่อน:

| # | ข้อเท็จจริง | ผลต่อการตรวจ |
|---|---|---|
| 1 | **ยังไม่เคยตรวจโบรกเกอร์จริงแม้แต่รายเดียว** | ดาว คะแนน และอันดับทุกจุดที่เห็นบนต้นแบบเป็น **ข้อมูลสมมติ** ที่สร้างขึ้นเพื่องานออกแบบ ไม่ใช่ผลตรวจ |
| 2 | **เว็บไซต์ยังไม่เปิดใช้งานจริง** | สิ่งที่มีคือ *ต้นแบบกดใช้งานได้* (prototype) 20 หน้าในไฟล์เดียว ไม่ใช่ระบบที่ให้บริการอยู่ |
| 3 | **ไม่มีเอกสารฉบับใดที่ระบุว่าผ่านการอนุมัติแล้ว** | เอกสารหลักทุกฉบับรวมถึงเอกสารตัวตน (`CORE-PILLAR.md`) ยังเป็น *ฉบับร่างรออนุมัติ* |
| 4 | **เกณฑ์การให้ดาวมีสองฉบับที่ขัดกันอยู่และยังไม่ชี้ขาด** | ดูข้อ 9.2 — มีผลต่อการตีความผลคะแนนทั้งระบบ |
| 5 | **ยังไม่มีนโยบายความเป็นส่วนตัวและเงื่อนไขการใช้งาน** | ทั้งที่หน้าสมัครสมาชิกในต้นแบบให้ผู้ใช้ติ๊กยอมรับทั้งสองอย่าง ดูข้อ 9.5 |

ตัวเลขปีในเอกสารทีมใช้ **พุทธศักราช (2569)** ส่วนตัวเลขปีบนหน้าเว็บต้นแบบใช้ **คริสต์ศักราช (2026)** — เป็นปีเดียวกัน

---

## 1. ภาพรวมโปรเจ็ค

### 1.1 โปรเจ็คนี้คืออะไร

RedStarTrust คือความพยายามสร้าง **สถาบันจัดเรทติ้งโบรกเกอร์การเงิน** ที่ตัดสินด้วยหลักฐานที่ตรวจสอบย้อนกลับได้
ไม่ใช่เว็บรวมความเห็นผู้ใช้ และไม่ใช่เว็บแนะนำโบรกที่รับค่าคอมมิชชั่น

**เสาหลัก 3 ข้อที่ทุกอย่างต้องรับใช้** (จาก `CORE-PILLAR.md` ฉบับร่าง 0.4):

1. **เป็นแหล่งศูนย์กลางข้อมูลโบรกเกอร์** พร้อมประวัติย้อนหลังตลอดกาล ไม่ลบไม่ซ่อนของเก่า
2. **ทุกอย่างบนเว็บต้องนำไปสู่การตัดสินใจเลือกโบรก** ไม่มีข้อมูลที่ใส่ไว้ "เพื่อให้ครบ"
3. **คิดในมุมมองสถาบัน** — มีระเบียบวิธีเป็นลายลักษณ์อักษร ทำซ้ำได้ ไม่ขึ้นกับตัวบุคคล

### 1.2 หลักการตรวจที่ประกาศไว้ (ยังไม่ถูกนำไปใช้จริง)

| # | หลักการ | ความหมาย |
|---|---|---|
| 1 | ตรวจแบบลูกค้าทั่วไปแฝงตัว | ผู้ตรวจไม่มีสถานะพิเศษ ไม่ขอเอกสารภายใน ไม่สัมภาษณ์ผู้บริหาร |
| 2 | หลักฐานมาก่อนความเห็น | ทุกข้อตัดสินด้วยภาพหน้าจอ เอกสาร บันทึกเวลา ที่เก็บไว้ได้ |
| 3 | ผ่านครบทุกข้อ ไม่มีข้อยกเว้น | โมเดลแบบมิชลิน — บกพร่องข้อเดียวก็ไม่ได้ดาว |

**บททดสอบเดียวที่ใช้คัดข้อตรวจ:** *"ลูกค้ารายย่อยที่ไม่มีสถานะพิเศษ ทำข้อนี้ได้จริงไหม"* — ตอบไม่ได้ = ข้อนั้นตกทันที

*อ้างอิง: `CORE-PILLAR.md` ข้อ 1–3*

---

## 2. ขอบเขตงาน

### 2.1 อยู่ในขอบเขต

- ระบบเกณฑ์ตรวจโบรกเกอร์ CFD/Forex — เขียนเสร็จแล้ว 3,000 ข้อ
- ต้นแบบเว็บไซต์ที่แสดงผลตรวจ อันดับ บทรีวิว และรางวัล
- ระบบรางวัล "ดาวแดง" พร้อมใบรับรองและหน้าตรวจสอบใบรับรอง
- เครื่องมือ EA ที่เก็บข้อมูลต้นทุนจริงจากการเทรดของผู้ใช้ (ออกแบบแล้ว ยังไม่พัฒนา)
- เนื้อหาข่าวและบทความประกอบ (มีตัวอย่างจริง 9 เรื่อง สองภาษา)

### 2.2 อยู่นอกขอบเขต (ระบุไว้ชัดในเอกสารทีม)

- **ไม่ทำหน้าที่หน่วยงานกำกับดูแล** — ไม่มีอำนาจตามกฎหมาย ไม่ลงโทษใคร ไม่บังคับให้โบรกคืนเงิน
- **ไม่ให้คำแนะนำการลงทุนรายบุคคล**
- **ไม่รับเงินแลกกับดาวหรืออันดับ** — รายได้ต้องไม่ผูกกับผลคะแนน
- ยังไม่ขยายไปโบรกหุ้น/กองทุนรวมในเฟสนี้ (เตรียมเอกสารอ้างอิงไว้แล้วแต่ยังไม่เริ่ม)

---

## 3. ผลผลิตของโปรเจ็ค

| รหัส | ผลผลิต | คืออะไร | สถานะจริง |
|---|---|---|---|
| A | **ระบบให้ดาว** | ผ่านครบทุกข้อในชุดตรวจ = ได้ดาว | เกณฑ์เขียนครบ · **ยังไม่เคยตรวจโบรกจริง** |
| B | **จัดอันดับรายประเด็น** | เทียบโบรกในประเด็นเดียว เช่น ต้นทุนต่ำสุด ถอนไวสุด | มีสูตรต้นแบบ · หน้าเว็บทำเฉพาะอันดับรายประเทศ |
| C | **บทรีวิวโดยผู้เชี่ยวชาญ** | บริบทที่ตัวเลขเล่าไม่ได้ ต้องอ้างหลักฐานจาก A/B เสมอ | โครงหน้าเว็บ 10 หัวข้อเสร็จ · **เนื้อหาจริงยังไม่มี** |
| D | **ประวัติย้อนหลังรายโบรก** | ไทม์ไลน์ดาว ผลตรวจ และเหตุการณ์สำคัญทุกไตรมาส | ทำแล้วเฉพาะ "ประวัติรางวัล" ยังไม่ครบตามที่เสาหลักข้อ 1 เรียกร้อง |

**กฎคุมความเป็นกลางที่เขียนไว้:** ผลผลิต C (ความเห็นผู้เชี่ยวชาญ) **ห้ามเปลี่ยนดาวหรืออันดับ** ที่คำนวณจาก A/B ได้ และต้องติดป้ายแยกให้ผู้อ่านรู้ว่าเป็นความเห็น ไม่ใช่ผลตรวจ

---

## 4. โครงสร้างการทำงานและบทบาท

```
Boss (เจ้าของโปรเจ็ค · ผู้ตัดสินใจและอนุมัติทุกเรื่อง)
  └── Doing — General Manager : แปลงคำสั่งเป็นเอกสาร คุมเกณฑ์ GCSI
        ├── Going  — Chief of Staff : งานปริมาณมาก เขียนข้อตรวจ ติดตามผล
        ├── Moving — งานเขียน : บทรีวิวโบรกเกอร์และข่าว
        └── Noting — UX/UI : โครงสร้างเว็บ หน้าจอ ต้นแบบ
```

**กฎการทำงานที่บังคับใช้กับทุกบทบาท** (จาก `*/CLAUDE.md` ของแต่ละตำแหน่ง):

1. ห้ามทำสิ่งที่ไม่ได้รับคำสั่ง — คำสั่งกำกวมต้องถามก่อน ไม่เดาแล้วทำ
2. งานที่ย้อนกลับยาก (ลบข้อมูล · ส่งข้อมูลออกนอกเครื่อง · **เผยแพร่สู่สาธารณะ**) ต้องขออนุมัติทุกครั้ง
3. รายงานตามจริง — ทำไม่สำเร็จหรือข้ามขั้นตอนไหนต้องบอกตรง ๆ
4. ห้ามตัดสินใจแทนผู้มีอำนาจในเรื่องผูกพัน (ทิศทางแบรนด์ · โครงสร้างเว็บ · ฟีเจอร์ที่จะมี/ไม่มี)
5. **ห้ามออกแบบหน้าจอที่กล่าวอ้างข้อมูลโบรกที่ยังไม่ได้ตรวจ** — ตัวอย่างทุกจุดต้องระบุว่าเป็นข้อมูลสมมติ

ทุกบทบาทมีสมุดบันทึกงานของตัวเอง (`STATE.md`) ที่ต้องเขียนก่อนจบทุกรอบงาน เพื่อให้งานต่อเนื่องได้แม้เปลี่ยนผู้ปฏิบัติ

---

## 5. กลไกควบคุมคุณภาพและความเป็นกลาง

กลไกเหล่านี้ **เขียนเป็นกฎไว้แล้วและใช้จริงในไฟล์ข้อตรวจ** — ผู้ตรวจสามารถสุ่มเปิดไฟล์ใดก็ได้ใน `gcsi/pillars/` เพื่อยืนยัน

| กลไก | รายละเอียด | หลักฐานในรีโป |
|---|---|---|
| **มาตรฐานการเขียนข้อตรวจ** | ทุกข้อต้องมีครบ: ตรวจอะไร · ขั้นตอนทีละขั้น · หลักฐานที่ต้องเก็บ · เกณฑ์ผ่าน · เกณฑ์ไม่ผ่าน · นิยาม "ยังไม่ได้ตรวจ" · ตัวอย่าง | `gcsi/INSPECTION-STANDARD.md` |
| **ห้ามแต่งตัวเลข ห้ามตั้งเกณฑ์หลังเห็นผล** | จุดที่ต้องใช้ตัวเลขตัดสินแต่ยังไม่มีตัวเลขอนุมัติ ต้องเขียนเป็น `[เกณฑ์เสนอ: ...]` แล้วรวมไว้ในทะเบียนกลางให้ผู้มีอำนาจเคาะ | `gcsi/PENDING-CRITERIA.md` |
| **รหัสหลักฐาน** | หลักฐานทุกชิ้นมีรหัสรูปแบบเดียวกัน เช่น `RST-BRKA-P01-001-01` เพื่อสอบกลับได้ | `gcsi/pillars/*.md` |
| **กันข้อตรวจซ้ำ** | แบ่งงาน 10 เสา × 300 ข้อ ลง 60 ไฟล์ ไฟล์ละ 50 ข้อ มีผังกำกับ | `gcsi/ITEM-MAP.md` |
| **เปิดเผยเกณฑ์ต่อสาธารณะ** | คัด 1,000 ข้อจาก 3,000 ข้อ (เสาละ 100 ข้อ) ไว้เผยแพร่ | `gcsi/checklist-1000-public.md` |
| **ห้ามใช้ชื่อโบรกจริงในบริบทเชิงลบ** | ตัวอย่างเชิงลบทุกจุดใช้ชื่อสมมติ (Apex Global FX, Kestrel FX ฯลฯ) | ต้นแบบเว็บ + ไฟล์ข้อตรวจ |
| **ติดป้ายข้อมูลสมมติบนหน้าเว็บ** | ทุกหน้าที่มีตัวเลขตัวอย่างเขียนกำกับว่าเป็นข้อมูลสมมติ ยังไม่ผ่านการตรวจจริง | ต้นแบบเว็บทุกหน้า |
| **กำแพงกั้นรายได้** | ปุ่มเปิดบัญชีบนหน้ารีวิวกำกับไว้ว่าเป็นลิงก์ไปเว็บทางการของโบรกโดยตรง **ไม่ใช่ลิงก์แนะนำที่ได้ค่าคอมมิชชั่น** | หน้ารีวิวรายโบรกในต้นแบบ |

---

## 6. สถานะปัจจุบันของงาน

| งาน | สถานะ | ตัวเลขที่ยืนยันได้ |
|---|---|---|
| เอกสารตัวตนและกลยุทธ์ | เขียนแล้ว รออนุมัติ | 5 ฉบับ |
| เกณฑ์ตรวจ GCSI | **เขียนครบแล้ว** รออนุมัติ | 10 เสา · 60 ไฟล์ · **3,000 ข้อพอดี** |
| ชุดเกณฑ์ฉบับเปิดเผยสาธารณะ | คัดแล้ว รออนุมัติ | 1,000 ข้อ |
| ทะเบียนตัวเลขเกณฑ์รออนุมัติ | รวบรวมแล้ว รอผู้มีอำนาจเคาะ | `PENDING-CRITERIA.md` |
| ต้นแบบเว็บไซต์ | ใช้งานได้จริงในเบราว์เซอร์ | **20 หน้า** ในไฟล์เดียว |
| ผังเว็บและกลุ่มผู้ใช้ | จัดทำแล้ว 2 ก.ย. 2569 | `Noting/design/sitemap.html` · `user-info.html` |
| สเปกงานออกแบบ | 6 ฉบับ รออนุมัติ | ฉบับใหญ่สุด 53 KB (ระบบรางวัล) |
| เนื้อหาข่าว | มีของจริงแล้ว | 9 เรื่อง ไทย–อังกฤษ พร้อมภาพปก |
| บทรีวิวโบรกเกอร์ | **ยังไม่มีเนื้อหาจริง** | มีแต่โครง 10 หัวข้อ |
| เครื่องมือ EA | ออกแบบแล้ว **ยังไม่พัฒนา** | ปุ่มดาวน์โหลดในต้นแบบยังไม่มีไฟล์จริง |
| การตรวจโบรกจริง | **ยังไม่เริ่ม** | 0 ราย |
| เว็บไซต์ที่เปิดใช้จริง | **ยังไม่มี** | มีเฉพาะ preview ภายใน |

**ปริมาณงานรวมในรีโป:** ไฟล์เอกสาร 163 ไฟล์ · ประวัติการเปลี่ยนแปลง 34 ครั้งใน Git (เริ่มบันทึกลง Git เมื่อ 28 ส.ค. 2569 — งานช่วง 13–27 ส.ค. ทำก่อนเริ่มใช้ Git จึงไม่มีประวัติรายครั้ง)

---

## 7. ไทม์ไลน์ที่เกิดขึ้นจริง

| ช่วงเวลา | เหตุการณ์ |
|---|---|
| 13–14 ส.ค. 2569 | ตั้งเอกสารตัวตนฉบับแรก · วางโครงเกณฑ์ GCSI 2.0 · กำหนดมาตรฐานการเขียนข้อตรวจ |
| 15 ส.ค. 2569 | **ตัดสินใจเชิงหลักการ: เลิกใช้ระบบถ่วงน้ำหนัก** เปลี่ยนเป็น "ผ่านครบทุกข้อจึงได้ดาว" · คัดชุดเปิดเผย 1,000 ข้อ |
| 15–17 ส.ค. 2569 | เขียนข้อตรวจครบ 3,000 ข้อ · บันทึกหัวข้อที่ยังไม่ตัดสิน |
| 17–18 ส.ค. 2569 | ปรับเอกสารตัวตนถึงฉบับร่าง 0.4 |
| 20–25 ส.ค. 2569 | วิเคราะห์คู่แข่ง 2 ราย · ถอดระบบให้คะแนนของเว็บอ้างอิง · ร่างผังเว็บและฟีเจอร์ครั้งแรก · รับโจทย์เครื่องมือ EA |
| 21 ส.ค. 2569 | เสนอ GCSI 3.0 (ย้อนกลับไปใช้ระบบถ่วงน้ำหนัก) — **ยังไม่ได้ข้อสรุป** |
| 26–31 ส.ค. 2569 | งานออกแบบเข้มข้น: หน้าแรก · ระบบรางวัล · พื้นที่ข้อพิพาท · ทะเบียนค้นหา · ต้นแบบเว็บโตจาก 8 เป็น 20 หน้า |
| 1–3 ก.ย. 2569 | ปรับระบบดาวและหน้ารีวิว · เตรียมขึ้น Cloudflare preview · จัดทำผังเว็บ กลุ่มผู้ใช้ และแผนที่เอกสาร |

---

## 8. แผนงานที่เหลือ

> **หมายเหตุสำคัญ:** ยังไม่มีการกำหนดวันแล้วเสร็จของเฟสใดเลย เพราะทุกเฟสขึ้นกับการตัดสินใจที่ยังค้างอยู่ในข้อ 9
> ลำดับด้านล่างคือ *ลำดับที่จำเป็นทางตรรกะ* ไม่ใช่กำหนดการที่ผูกพัน

### เฟส 0 · ปิดการตัดสินใจที่ค้าง (ต้องทำก่อนทุกอย่าง)

ชี้ขาดเกณฑ์การให้ดาวว่าใช้ฉบับใด · เคาะตัวเลขเกณฑ์ในทะเบียน · อนุมัติหรือแก้เอกสารตัวตน
**ถ้าไม่ปิดเฟสนี้ งานตรวจจริงเริ่มไม่ได้** เพราะจะกลายเป็นการตั้งเกณฑ์หลังเห็นผล ซึ่งขัดกฎของโปรเจ็คเอง

### เฟส 1 · ปิดช่องว่างที่ผู้ใช้ต้องเห็น

จัดทำหน้าที่ยังไม่มีแต่จำเป็นต่อความน่าเชื่อถือ: หน้าเกี่ยวกับสถาบัน · ระเบียบวิธีฉบับเปิดเผย ·
**เงื่อนไขการใช้งานและนโยบายความเป็นส่วนตัว** · ช่องทางติดต่อ · หน้าค้นหา

### เฟส 2 · ตรวจโบรกเกอร์ชุดแรกจริง

คัดโบรกชุดแรก · จัดหาผู้ตรวจและกระบวนการสอบทานให้ได้ผลตรงกันระหว่างผู้ตรวจสองคน ·
เก็บหลักฐานตามรหัส · ทดสอบการฝาก–ถอนจริง · สรุปผลรอบแรก

### เฟส 3 · เชื่อมข้อมูลจริงเข้าเว็บและเปิดใช้

แทนข้อมูลสมมติทั้งหมดด้วยผลตรวจจริง · ตัดสินโครงสร้าง URL ถาวร · เปิดระบบสมาชิกและการรับเรื่องร้องเรียน

### เฟส 4 · ขยายสินค้าและวงจรทบทวน

ขยายเกณฑ์ไปโบรกหุ้น/กองทุนรวมโดยเริ่มจากโครง 10 เสาเดิม · ตั้งรอบทบทวนเกณฑ์ของตัวเอง

---

## 9. ทะเบียนความเสี่ยงและประเด็นที่ยังไม่ตัดสิน

### 9.1 ยังไม่มีเอกสารฉบับใดได้รับการอนุมัติอย่างเป็นทางการ
งานทั้งหมดยืนอยู่บนฐานที่ยังไม่ถูกล็อก ถ้าเอกสารตัวตนถูกแก้ในภายหลัง งานที่ทำไปแล้วอาจต้องรื้อ
**ผู้ตัดสิน:** Boss

### 9.2 เกณฑ์การให้ดาวมีสองฉบับที่ขัดกัน
`WEIGHTS.md` ฉบับ 2.0 (15 ส.ค.) กำหนดว่า **เลิกถ่วงน้ำหนัก ผ่านครบทุกข้อจึงได้ดาว**
`GCSI-3.0-PROPOSAL.md` (21 ส.ค.) เสนอ **ย้อนกลับไปใช้ระบบถ่วงน้ำหนัก** พร้อมด่านบังคับ 6 ข้อ และถอดหมวดหนึ่งออก
ตราบใดที่ยังไม่ชี้ขาด หน้าเว็บที่อธิบายเกณฑ์ก็อ้างอิงกฎที่อาจถูกยกเลิก
**ผลกระทบ:** สูง — กระทบการตีความผลคะแนนทั้งระบบ · **ผู้ตัดสิน:** Boss

### 9.3 ตัวเลขเกณฑ์จำนวนมากยังไม่ได้รับอนุมัติ
รวบรวมไว้ครบใน `PENDING-CRITERIA.md` แล้ว แต่ยังไม่มีใครเคาะ
**ผลกระทบ:** สูง — ตรวจจริงไม่ได้จนกว่าจะเคาะ เพราะจะกลายเป็นการตั้งเกณฑ์หลังเห็นผล

### 9.4 สิทธิ์การใช้โลโก้โบรกเกอร์ยังไม่มีข้อสรุป
ต้นแบบใช้โลโก้จริงของโบรกเกอร์ที่ดึงจากโดเมนทางการของแต่ละราย เรื่องเสนอขอสิทธิ์เขียนไว้แล้วแต่ยังไม่ได้คำตอบ
และยังไม่ตัดสินว่า **โบรกที่ถูกเพิกถอนดาวจะยังแสดงโลโก้ต่อหรือไม่**
**ผลกระทบ:** ปานกลาง–สูง (ประเด็นกฎหมาย) · **หลักฐาน:** `Noting/ASK-DOING-LOGO-RIGHTS.md`

### 9.5 ยังไม่มีนโยบายความเป็นส่วนตัวและเงื่อนไขการใช้งาน
หน้าสมัครสมาชิกให้ผู้ใช้ติ๊กยอมรับเอกสารสองฉบับที่ยังไม่มีตัวตน และเครื่องมือ EA ออกแบบไว้ให้เก็บข้อมูลการเทรดของผู้ใช้
รวมถึงระบบร้องเรียนที่ให้ผู้ใช้อัปโหลดหลักฐานส่วนบุคคล
**ผลกระทบ:** สูง (ประเด็นกฎหมายคุ้มครองข้อมูล) — ควรปิดก่อนเปิดระบบสมาชิกจริง

### 9.6 ยังไม่มีขั้นตอนยืนยันตัวตนของตัวแทนโบรกเกอร์
ในต้นแบบ ผู้สมัครเลือกเองได้ว่าเป็น "ตัวแทนโบรกเกอร์" แล้วเข้าหน้าชี้แจงได้ทันที
ถ้าใครก็อ้างเป็นโบรกได้ คำชี้แจงที่แสดงคู่กับผลตรวจจะเชื่อถือไม่ได้ทั้งระบบ
**ผลกระทบ:** สูง ต่อความน่าเชื่อถือ

### 9.7 ยังไม่มีกระบวนการสอบทานผู้ตรวจ
เอกสารประกาศว่าผลตรวจต้อง **ทำซ้ำได้ ผู้ตรวจคนละคนต้องได้ผลเดียวกัน** แต่ยังไม่มีขั้นตอนคาลิเบรตหรือสุ่มสอบทานจริง
**ผลกระทบ:** สูง — เป็นข้อที่หน่วยงานตรวจสอบน่าจะให้น้ำหนักมากที่สุด

### 9.8 ไฟล์ต้นแบบมีแหล่งแก้ไขสองทางที่ทับกันเอง
ไฟล์เว็บที่ใช้แสดงผลถูกแก้สองทาง — ทางหนึ่งสร้างใหม่จากสคริปต์ อีกทางแก้ที่ไฟล์ผลลัพธ์โดยตรง
เคยทำให้งานออกแบบหายไปแล้วหนึ่งครั้งและต้องกู้คืนจากประวัติ Git และสำเนาที่เตรียมไว้ขึ้นเซิร์ฟเวอร์ก็ยังไม่ตรงกับไฟล์ออกแบบ
**ผลกระทบ:** ปานกลาง (ความเสี่ยงเชิงกระบวนการ ไม่ใช่เชิงเนื้อหา)

### 9.9 ฉบับภาษาอังกฤษยังแปลไม่ครบ
ป้ายอินเทอร์เฟซแปลแล้ว แต่บทความและรีวิวยังเป็นภาษาไทย โดยหน้าเว็บเขียนกำกับสถานะนี้ไว้ตรง ๆ
เทียบกับเป้าหมายที่ประกาศว่าเป็นมาตรฐานระดับโลก ถือเป็นช่องว่างที่ต้องมีแผนปิด

### 9.10 Core Loop ยังไม่เคยถูกเขียนเป็นเอกสารทางการ
วงจร Collect → Analyze → Decide → Update ซึ่งเป็นเหตุผลว่าทำไมต้องมีเครื่องมือ EA
ปรากฏอยู่ในบรีฟฉบับหนึ่งเท่านั้น และตัวบรีฟเองก็เขียนเตือนไว้ว่าควรทำเป็นเอกสารทางการ

---

## 10. สิ่งที่หน่วยงานตรวจสอบยังตรวจไม่ได้ในขณะนี้

เพื่อความชัดเจน ขอระบุว่า **ยังไม่มีสิ่งเหล่านี้ให้ตรวจ**:

- ผลการตรวจโบรกเกอร์จริงและแฟ้มหลักฐานประกอบ — ยังไม่มีการตรวจใดเกิดขึ้น
- บันทึกการสอบทานระหว่างผู้ตรวจ — ยังไม่มีกระบวนการ
- นโยบายความเป็นส่วนตัว เงื่อนไขการใช้งาน และข้อตกลงกับโบรกเกอร์
- โครงสร้างองค์กร งบประมาณ และแหล่งรายได้ที่เป็นทางการ
- ระบบที่เปิดใช้งานจริง รวมถึงระบบสมาชิกและการรับเรื่องร้องเรียน
- ซอฟต์แวร์เครื่องมือ EA — มีเฉพาะการออกแบบหน้าจอ

**สิ่งที่ตรวจได้ตอนนี้:** ตัวเกณฑ์ 3,000 ข้อและมาตรฐานการเขียน · เอกสารตัวตนและกฎความเป็นกลาง ·
ต้นแบบเว็บที่แสดงว่าผลตรวจจะถูกนำเสนออย่างไร · ทะเบียนประเด็นที่ยังไม่ตัดสิน · ประวัติการทำงานใน Git

---

## 11. ภาคผนวก — แผนที่ไฟล์สำหรับผู้ตรวจ

| ต้องการดู | เปิดที่ |
|---|---|
| ตัวตนและหลักการทั้งหมด | `CORE-PILLAR.md` |
| กฎการเขียนข้อตรวจ | `gcsi/INSPECTION-STANDARD.md` |
| ข้อตรวจจริง 3,000 ข้อ | `gcsi/pillars/p01-01.md` … `p10-06.md` |
| ผังการแบ่งข้อตรวจ | `gcsi/ITEM-MAP.md` |
| กฎการให้ดาวฉบับปัจจุบัน | `gcsi/WEIGHTS.md` |
| ข้อเสนอเปลี่ยนเกณฑ์ที่ยังไม่ชี้ขาด | `gcsi/GCSI-3.0-PROPOSAL.md` |
| ตัวเลขเกณฑ์ที่รออนุมัติ | `gcsi/PENDING-CRITERIA.md` |
| ชุดเกณฑ์ที่จะเปิดเผยต่อสาธารณะ | `gcsi/checklist-1000-public.md` |
| ต้นแบบเว็บไซต์ | `Noting/design/redstartrust-homepage-mockup.html` |
| ผังเว็บและเส้นเชื่อมระหว่างหน้า | `Noting/design/sitemap.html` |
| กลุ่มผู้ใช้และเส้นทางการใช้งาน | `Noting/design/user-info.html` |
| แผนที่เอกสารทั้งโปรเจ็ค + ขั้นตอนการทำงาน | `Noting/design/project-docs.html` |
| บันทึกงานออกแบบทุกรอบ | `Noting/STATE.md` |
| กฎการทำงานของแต่ละบทบาท | `Doing/CLAUDE.md` · `Going/CLAUDE.md` · `Moving/CLAUDE.md` · `Noting/CLAUDE.md` |

---

**สถานะเอกสารฉบับนี้:** จัดทำเพื่อส่งให้หน่วยงานตรวจสอบตามคำสั่ง Boss · ยังไม่ผ่านการอนุมัติจาก Boss
**ผู้จัดทำ:** Noting (นักออกแบบ UX/UI) · **วันที่:** 3 กันยายน 2569
**ข้อจำกัดความรับผิด:** ตัวเลข คะแนน ดาว และชื่อที่ปรากฏบนต้นแบบเว็บทั้งหมดเป็นข้อมูลสมมติเพื่องานออกแบบ
ไม่ใช่ผลการตรวจสอบโบรกเกอร์รายใด และไม่ควรถูกอ้างอิงเป็นข้อเท็จจริงเกี่ยวกับผู้ให้บริการรายใดทั้งสิ้น

# ข้อตกลงแบ่งไฟล์ในทีม — ใครดูแลอะไร

> **Boss เคาะเมื่อ 3 กันยายน 2569** หลังเกิดเหตุแก้ไฟล์เดียวกันจนงานเกือบหาย
> ทุกคนที่ commit เข้า repo นี้ต้องอ่านก่อนเริ่มงาน

---

## ทำไมต้องมีเอกสารนี้

วันที่ 2–3 ก.ย. 69 มีสองฝ่ายแก้ไฟล์ **`Noting/design/redstartrust-homepage-mockup.html`** พร้อมกัน

- **thunchaaaa** แก้ไฟล์นี้**โดยตรง** (ออกแบบหน้าแรกใหม่)
- **Noting** แก้ **ไฟล์ซอร์ส** แล้วสั่ง build ซึ่ง**สร้างไฟล์นี้ทับทั้งไฟล์**

ผลคือใครสั่ง build ทีหลัง งานอีกฝ่าย**หายทันทีโดยไม่มี error เตือน**
ตรวจแล้วพบว่าต่างกัน **63.8 KB** — คืองานออกแบบหน้าแรกทั้งหมดที่ไม่ได้อยู่ในซอร์ส

---

## ใครดูแลอะไร

### thunchaaaa — ฝั่งเผยแพร่

| ไฟล์ / โฟลเดอร์ | หมายเหตุ |
|---|---|
| `public/` | เว็บที่ deploy จริง (`index.html`, `logos/`) |
| `wrangler.jsonc` · `package.json` | คอนฟิก Cloudflare |
| `CLOUDFLARE-DEPLOY.md` | คู่มือ deploy |
| `Noting/design/ranking-section-10-options.html` | ไฟล์ทดลองของตัวเอง |

### Noting — ฝั่งออกแบบต้นแบบ

| ไฟล์ / โฟลเดอร์ | หมายเหตุ |
|---|---|
| `Noting/design/Main.dc.html` | **ซอร์ส** — โครงหน้า |
| `Noting/design/build-mockup.py` | **ซอร์ส** — CSS + JS + ข้อมูล |
| `Noting/design/redstartrust-homepage-mockup.html` | **ไฟล์ผลลัพธ์** สร้างจากซอร์ส |
| `Noting/design/_preview.html` · `logos/` | ไฟล์ประกอบ |
| `Noting/*.md` | เอกสารของ Noting |

---

## กฎเหล็ก 3 ข้อ

**1. ห้ามข้ามฝั่งไปแก้ไฟล์ของอีกคน**
ถ้าจำเป็นต้องแก้ ให้บอกเจ้าของไฟล์ก่อน อย่าแก้เงียบ ๆ

**2. `redstartrust-homepage-mockup.html` เป็นไฟล์ผลลัพธ์ — ห้ามแก้ด้วยมือ**
มันถูกสร้างใหม่ทุกครั้งที่รัน `build-mockup.py` แก้มือไปเท่าไหร่ก็หายหมด
**อยากเปลี่ยนอะไรในนั้น ต้องแก้ที่ `Main.dc.html` หรือ `build-mockup.py`**

**3. งานออกแบบที่อยากให้อยู่ถาวร ต้องอยู่ในซอร์ส**
ถ้าแก้แค่ไฟล์ผลลัพธ์หรือ `public/index.html` งานนั้นจะหายรอบหน้า

---

## ส่งงานกันยังไง

```
Noting แก้ซอร์ส
   ↓  python build-mockup.py
Noting/design/redstartrust-homepage-mockup.html      ← ของ Noting
   ↓  Noting บอกว่า "รอบใหม่พร้อมแล้ว"
thunchaaaa คัดลอกไปเป็น public/index.html            ← ของ thunchaaaa
   ↓  npm run deploy
เว็บจริง
```

**ฝั่ง Noting ไม่แตะ `public/` · ฝั่ง thunchaaaa ไม่แตะไฟล์ในโฟลเดอร์ `Noting/design/` ที่เป็นซอร์สและผลลัพธ์**

ถ้า thunchaaaa อยากปรับดีไซน์เพิ่มหลังรับไฟล์มา ทำได้ใน `public/index.html`
แต่ต้องรู้ว่า **รอบหน้า Noting ส่งไฟล์ใหม่มา การปรับนั้นจะไม่ติดมาด้วย**
ถ้าอยากให้ติดถาวร ให้บอก Noting เอาไปใส่ในซอร์ส

---

## ยามกันพลาด

`build-mockup.py` มีตัวตรวจแล้ว — ถ้าไฟล์ผลลัพธ์บนดิสก์**ใหญ่กว่าที่กำลังจะเขียนเกิน 8 KB**
แปลว่าอาจมีงานคนอื่นอยู่ในนั้น สคริปต์จะ**หยุดทันที ไม่เขียนทับ** และสำรองไฟล์เดิมไว้ที่
`redstartrust-homepage-mockup.html.before-build` พร้อมบอกวิธีแก้

ถ้าเจอข้อความ `STOP:` **อย่าลบไฟล์ทิ้งเพื่อให้ build ผ่าน** — ให้ไปคุยกับเจ้าของงานก่อน

---

## สถานะไฟล์ ณ วันที่เขียน (3 ก.ย. 69)

| ไฟล์ | มีอะไรอยู่ |
|---|---|
| `Noting/design/redstartrust-homepage-mockup.html` | **ครบทั้งสองฝ่าย** — Hero ใหม่ของ thunchaaaa + งาน Noting 17 อย่างของวันที่ 3 ก.ย. |
| `public/index.html` (เว็บจริง) | **ยังเก่า** — มี Hero ของ thunchaaaa แต่**ไม่มีงาน Noting ของวันที่ 3 ก.ย.** และยังมีหน้า Review กับบล็อกเทียบที่ถูกลบไปแล้ว |
| `Main.dc.html` + `build-mockup.py` (ซอร์ส) | มีงาน Noting ครบ แต่ **ยังไม่มี Hero ใหม่ของ thunchaaaa** |

**เรื่องที่ยังไม่จบ รอ Boss ตัดสิน:** Hero หน้าแรกที่ thunchaaaa ออกแบบยังไม่ได้อยู่ในซอร์ส
ตราบใดที่ยังไม่ย้ายเข้าไป ฝั่ง Noting จะ build ไม่ได้ (ยามจะหยุดไว้ทุกครั้ง)
ต้องเลือกอย่างใดอย่างหนึ่ง — **ย้าย Hero เข้าซอร์ส** หรือ **ตกลงว่า Hero เป็นชั้นที่ thunchaaaa ทาทับเองใน `public/` ทุกรอบ**
